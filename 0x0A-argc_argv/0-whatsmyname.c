#include <stdio.h>

/**
 * main - prints its name with \n
 * @argc: number
 * @argv: array string
 * Return: 0
 */
int main(int argc, char *argv[])
{
	argc = 0;

	printf("%s\n", argv[argc]);

	return (0);
}
