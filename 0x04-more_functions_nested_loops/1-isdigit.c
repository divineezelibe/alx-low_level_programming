#include "main.h"

/**
 * _isupper - Checks for uppercase characters
 * @c: Integer parameter
 *
 * Return: 1
 */

int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
		return (1);
	else
		return (0);
}
