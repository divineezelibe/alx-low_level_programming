#include "main.h"

/**
 * add - Add two integers and return the result
 * @one: number one parameter
 * @two: number two parameter
 * Return: th result of the addition
 */

int add(int one, int two)
{
	return (one + two);
}
